#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using P = pair<int, int>;
typedef long long ll;

int main() {
  int h, w;
	cin >> h >> w;
	vector<string> c(h);
	rep(i,h){
		cin >> c[i];
		cout << c[i] << endl;
		cout << c[i] << endl;
	}
	return 0;
}