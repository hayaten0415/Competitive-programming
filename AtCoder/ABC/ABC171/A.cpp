#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using P = pair<int, int>;
typedef long long ll;

int main() {
  char s;
  cin >> s;
  if(s >= 'a' && s <= 'z'){
    cout << "a" << endl;
  }else{
    cout << "A" << endl;
  }
}