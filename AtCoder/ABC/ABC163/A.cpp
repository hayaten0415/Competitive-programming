#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main() {
  int r;
  cin >> r;
  double ans = (double)2 * r * M_PI;
  cout << fixed << ans << setprecision(10) << endl;
}